{
  "name": "all-the-lights-api-favorites",
  "version": "0.1.0",
  "description": "The backend favorites API for all-the-lights",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "nyc mocha --recursive test/",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.809.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0",
    "sinon": "^9.2.2"
  }
}
